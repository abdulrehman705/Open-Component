export interface HtmlTemplate {
    templateId: string;
    templateName: string;
    css: string;
    externals: Array<{
        name: string;
        global: string;
    }>;
    hash: string;
    bundle: string;
    componentHash: string;
}
export default function htmlTemplate({ templateId, templateName, externals, css, bundle, componentHash, hash, }: HtmlTemplate): string;
