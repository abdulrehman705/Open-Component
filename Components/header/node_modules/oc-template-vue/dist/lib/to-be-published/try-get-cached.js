"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const nice_cache_1 = __importDefault(require("nice-cache"));
const cache = new nice_cache_1.default({});
async function tryGetCached(type, key, predicate) {
    const cached = cache.get(type, key);
    if (cached) {
        return cached;
    }
    const data = await predicate();
    cache.set(type, key, data);
    return data;
}
exports.default = tryGetCached;
